{
  "name": "@studio/worker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@studio/database": "workspace:*",
    "@studio/media": "workspace:*",
    "@studio/workflow": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
