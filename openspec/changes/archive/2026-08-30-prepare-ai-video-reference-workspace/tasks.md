## 1. Repository setup

- [x] 1.1 Initialize the parent Git repository and add `/references` to `.gitignore`; verify `git status --ignored` reports the reference path as ignored.
- [x] 1.2 Create the root `docs/` directory for later research notes; verify the directory exists locally.

## 2. Reference acquisition

- [x] 2.1 Clone `harry0703/MoneyPrinterTurbo` into `references/MoneyPrinterTurbo/`; verify the clone has upstream Git metadata.
- [x] 2.2 Clone `jianchang512/pyvideotrans` into `references/pyvideotrans/`; verify the clone has upstream Git metadata.
- [x] 2.3 Clone `linyqh/NarratoAI` into `references/NarratoAI/`; verify the clone has upstream Git metadata.
- [x] 2.4 Clone `FujiwaraChoki/MoneyPrinter` into `references/MoneyPrinter/`; verify the clone has upstream Git metadata.
- [x] 2.5 Clone `RayVentura/ShortGPT` into `references/ShortGPT/`; verify the clone has upstream Git metadata.
- [x] 2.6 Clone `m-bain/whisperX` into `references/whisperX/`; verify the clone has upstream Git metadata.
- [x] 2.7 Clone `rany2/edge-tts` into `references/edge-tts/`; verify the clone has upstream Git metadata.
- [x] 2.8 Clone `SWivid/F5-TTS` into `references/F5-TTS/`; verify the clone has upstream Git metadata.
- [x] 2.9 Clone `RVC-Boss/GPT-SoVITS` into `references/GPT-SoVITS/`; verify the clone has upstream Git metadata.
- [x] 2.10 Clone `ZC89757/story-claw` into `references/story-claw/`; verify the clone has upstream Git metadata.

## 3. Documentation

- [x] 3.1 Write `references/README.md` with every project’s expected directory, GitHub URL, and primary purpose; verify all ten projects are listed.
- [x] 3.2 Write the root `README.md` describing the reference-workspace purpose, read-only boundary, and future AI Video Studio architecture research; verify it explicitly says application implementation is out of scope.

## 4. Verification

- [x] 4.1 Verify all ten reference directories exist, retain Git history, and remain excluded from the parent repository’s tracked file set.
