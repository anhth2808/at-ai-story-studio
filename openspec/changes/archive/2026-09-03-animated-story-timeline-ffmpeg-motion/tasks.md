## 1. Shared contracts and pure timeline logic

- [x] 1.1 Add strict shared schemas and DTOs for SceneTiming, MotionPlan, SceneTimelineItem, ChapterTimeline, ProjectTimeline, render scopes, visual source modes, fallback policies, quality/fitting/transition settings, RenderPlan, and hierarchical progress. Verify invalid enum, range, duration, and scope payloads fail the existing Zod boundary tests.
- [x] 1.2 Extend workflow step and asset type unions with timing, motion, Scene Clip, Chapter Video, and Project Video values while preserving legacy `RENDER` and `RENDERED_VIDEO`. Verify existing Story, Scene, image, TTS, subtitle, and background workflow tests still parse their prior values.
- [x] 1.3 Extend render configuration validation for 16:9/9:16, quality preset, fitting, motion intensity, transitions, subtitle position/outline, and explicit `SCENES`/`BACKGROUND` source with compatibility defaults. Verify valid landscape/portrait configs round-trip and unsafe settings are rejected before scheduling.
- [x] 1.4 Add source-span-aware narration preparation and segmentation while preserving current cleaned text and hashes. Verify CRLF/whitespace/control-character inputs retain deterministic UTF-16 source ranges and unmappable transformations fail explicitly.
- [x] 1.5 Implement pure SceneTiming mapping from source spans and cumulative measured TTS durations, including interpolation, gap closure, coverage validation, minimum-duration redistribution, AUTO/MANUAL modes, and timing diagnostics. Verify exact edge and inside-segment millisecond cases plus overlap/gap/stale-input failures.
- [x] 1.6 Implement pure deterministic MotionPlan selection from Scene framing, purpose, composition, movement intent, Scene number, and motion intensity. Verify identical inputs produce identical bounded plans, supported motions vary predictably, and subject-side constraints are respected.
- [x] 1.7 Implement pure 16:9/9:16 COVER and CONTAIN crop math with bounded normalized motion endpoints and intentional fill metadata. Verify incompatible source dimensions never distort, expose empty canvas, or produce out-of-bounds crop coordinates.

## 2. Database migration and repositories

- [x] 2.1 Add and register the next additive SQLite migration after `0012_image_candidates_quality.sql` for TTS source mappings, SceneTiming revisions/items, MotionPlan revisions, render scope/type/progress fields, and dependency indexes. Verify fresh and existing databases migrate without reset and preserve legacy rows.
- [x] 2.2 Update Drizzle schema types and constraints for the new timing, motion, render-job, and asset metadata fields. Verify generated queries typecheck and current-pointer/status constraints reject duplicate current revisions.
- [x] 2.3 Add repositories for current/history SceneTiming and MotionPlan revisions with transactional revision creation, manual lock reads, and stale detection. Verify historical revisions remain addressable after a new current revision.
- [x] 2.4 Extend asset repositories with hierarchical video roles/types, immutable manifest/output registration, fingerprint lookup, and dependency lineage helpers. Verify matching current assets can be found without reading media bytes and previous revisions remain non-current rather than deleted.
- [x] 2.5 Extend render-job persistence for `SCENE_CLIP`, `CHAPTER_VIDEO`, and `PROJECT_VIDEO` scope metadata, expected/actual duration, progress time, diagnostics, and output/timeline links. Verify one job record can represent each level and existing project render rows remain readable.
- [x] 2.6 Add repository tests for transaction rollback, current-pointer replacement, dependency insertion, scope-key uniqueness, and path-independent asset metadata. Verify a failed registration leaves the previous current output intact.

## 3. Centralized media compilation and validation

- [x] 3.1 Add validated media profile, Scene Clip input, Chapter assembly input, and Project assembly input types in `packages/media` without exposing raw filter text to callers. Verify malformed dimensions, durations, motion, transitions, and paths fail before process invocation.
- [x] 3.2 Implement the Scene Clip FFmpeg argument/filter compiler for looped still images, safe COVER/CONTAIN fitting, bounded Ken Burns motion, STATIC output, preview quality, H.264, FPS, and `yuv420p`. Verify compiler tests inspect argument arrays and a real short fixture produces the requested dimensions/duration.
- [x] 3.3 Implement Chapter Video assembly for CUT, CROSSFADE, and FADE, narration mapping, subtitle burn-in with bounded style values, authoritative duration padding/trimming, normalized H.264/AAC output, and no project music. Verify each transition has safe arguments and a real fixture keeps subtitle/audio duration aligned.
- [x] 3.4 Implement Project Video assembly for ordered compatible Chapter Videos, scope metadata, optional single music loop/trim/mix with narration priority, and copy-based assembly where compatible. Verify music is absent from Chapter output, applied once at Project output, and disabled music does not add an input.
- [x] 3.5 Extend FFmpeg progress handling with bounded structured `-progress pipe:1` parsing, cancellation, timeout, and process-runner callbacks while preserving shell-free argument execution. Verify progress parses out-time and completion values and cancellation terminates a test process without publishing output.
- [x] 3.6 Add hierarchical ffprobe validation for stream presence, duration tolerance, dimensions, FPS/pixel format, codec/profile compatibility, audio sample rate, and MP4 readability. Verify invalid streams and duration mismatches fail with bounded structured errors.
- [x] 3.7 Add managed staging/promotion helpers for Scene, Chapter, Project videos and JSON manifests using existing safe path/hash utilities. Verify partial files never become current and successful promotion records a SHA-256 hash and workspace-relative path.
- [x] 3.8 Keep the existing background `buildRenderArguments` path explicit and regression-tested. Verify a legacy background project still renders with its current audio/subtitle/music behavior and does not select Scene fallback implicitly.

## 4. Workflow planning, scheduling, and execution

- [x] 4.1 Add timing scheduling/application methods that read current chapter revision, source-positioned TTS segments, measured chapter audio, and current Scenes, then persist a validated SceneTiming revision. Verify missing source mappings produce a named preflight blocker and valid timing survives worker restart.
- [x] 4.2 Add deterministic MotionPlan scheduling/application methods for current Scene revisions and preserve manual plans unless replacement is explicitly requested. Verify changing a plan creates a revision and does not mutate Scene narrative/image-generation records.
- [x] 4.3 Implement hierarchical fingerprint builders for Scene Clips, Chapter Videos, and Project Videos with canonical JSON and compiler versions. Verify only direct ordered inputs/settings change each level's fingerprint and unrelated Chapters do not affect it.
- [x] 4.4 Implement `RenderPlanner` resolution for Scene, Chapter, range, selected-Chapter, and full-Story scopes with reusable/required/blocked counts and expected durations. Verify a synthetic 100-Chapter/500-Scene plan remains metadata-only and does not invoke FFmpeg or load media bytes.
- [x] 4.5 Implement explicit `autoBuild` materialization of timing, MotionPlan, Scene Clip, Chapter Video, and Project Video steps/dependency edges in one database transaction. Verify dry-run creates no jobs and auto-build creates only missing/stale work.
- [x] 4.6 Add accepted/current Scene image preflight and explicit `FAIL`, `HOLD_PREVIOUS`, `BLACK`, and `PROJECT_BACKGROUND` fallback handling with manifest visibility. Verify rejected/stale candidates fail by default and each opt-in fallback is named in the plan and output metadata.
- [x] 4.7 Add focused invalidation for Scene image/MotionPlan/timing, Chapter audio/subtitle, project music/settings, and legacy background changes. Verify one Scene invalidates only its clip, containing Chapter, and affected Project outputs; one subtitle/audio change preserves all Scene Clips and unrelated Chapters.
- [x] 4.8 Extend `WorkerExecutor` dispatch and guarded execution for Scene Clip, Chapter Video, and Project Video steps. Verify each method rechecks current fingerprints/dependencies, writes an immutable manifest, validates output, registers dependencies, and refuses to publish stale or partial results.
- [x] 4.9 Make worker progress, blocked prerequisites, failure diagnostics, retry, and cancellation visible at Scene/Chapter/Project levels. Verify a failed Scene Clip blocks its Chapter honestly and retrying it does not rerun completed sibling clips.
- [x] 4.10 Add idempotent restart recovery for output-registered-but-step-incomplete renders and expired leases. Verify stopping the worker after completed Scene/Chapter work reuses matching outputs and resumes only remaining work.
- [x] 4.11 Preserve legacy `scheduleRender` behavior while routing explicit `SCENES` requests through the hierarchical planner and explicit `BACKGROUND` requests through the legacy renderer. Verify old no-body render clients remain compatible and new requests never silently switch visual source.
- [x] 4.12 Add workflow integration tests using a counting fake media runner for cache reuse, scoped invalidation, dependency ordering, failed-unit retry, cancellation, restart recovery, project ranges, selected Chapters, and full-Story assembly. Verify all required observable status transitions and no duplicate matching jobs.

## 5. API surface

- [x] 5.1 Add chapter-scoped timeline read endpoints returning current Scene timing, MotionPlan, transition, accepted-image state, clip status, Asset URLs, progress, and safe blockers without absolute paths or binary JSON. Verify pagination/chapter scope keeps large responses bounded.
- [x] 5.2 Add timing and MotionPlan build/update endpoints with strict ownership, revision, manual-lock, and validation checks. Verify invalid manual timing preserves the previous valid revision and valid edits invalidate only video descendants.
- [x] 5.3 Add RenderPlan dry-run and execution endpoints for Scene, Chapter, range, selected Chapters, and full Story scopes with explicit source, auto-build, fallback, quality, and fitting fields. Verify dry-run has no side effects and execution returns persisted workflow/job identifiers.
- [x] 5.4 Add scoped current Scene Clip, Chapter Video, and Project Video metadata/download access through existing safe Asset serving. Verify responses expose status/fingerprint/scope and stream only managed validated Assets.
- [x] 5.5 Extend project/chapter status responses with hierarchical render counts, active stage, persisted progress, retry/cancel actions, timing lock, missing-image blockers, and safe errors. Verify a partial render is distinguishable from complete output.

## 6. Timeline and Render UI

- [x] 6.1 Update web navigation to `[Story][Scenes][Visual Bible][Images][Timeline][Audio][Render]` while retaining explicit background/music controls in Render. Verify existing Story, Scenes, Visual Bible, Audio, and legacy Render flows remain reachable.
- [x] 6.2 Add typed API client calls and state loading/error/empty handling for chapter timelines, RenderPlans, hierarchical status, timing updates, MotionPlan updates, and scoped render commands. Verify stale requests do not overwrite a newly selected project/chapter view.
- [x] 6.3 Implement chapter-scoped Timeline cards/list with accepted-image thumbnail or missing state, source excerpt/title, start, duration, motion, transition, current revisions, render status, preview, motion edit, and image-selection actions. Verify rejected candidates are never displayed as render inputs.
- [x] 6.4 Implement bounded MotionPlan controls and preview request using existing image/Asset routes; keep business logic in typed clients/hooks rather than a monolithic component. Verify invalid values are blocked and a saved plan updates status/fingerprint without changing Scene narrative fields.
- [x] 6.5 Implement AUTO/MANUAL timing display and native bounded timing controls with chapter duration, gap/overlap validation, manual lock/unlock, and visible warnings. Verify invalid edits keep the last valid timing and valid edits show the new revision.
- [x] 6.6 Implement Scene/Chapter/range/selected/full Story render controls with dry-run plan visibility, auto-build choice, explicit source/fallback, reusable/required counts, and named blockers. Verify a range request displays and schedules only the selected Chapters.
- [x] 6.7 Implement hierarchical progress, failure, retry, cancellation, and restart-safe polling for Scene Clips, Chapter Videos, and Project Video assembly. Verify a failed Scene shows a retry action and dependent status rather than a false completed Story state.
- [x] 6.8 Apply existing Vietnamese UI conventions, accessible labels/alt text, keyboard operation, visible status text, loading/error/empty states, 44 px actions, and narrow-width stacked layout. Verify the Timeline at approximately 375 px has no horizontal scrolling and does not rely on color or hover alone.
- [x] 6.9 Run browser verification against the actual web app for a real project: open Timeline, inspect a Chapter, edit valid/invalid timing, preview motion, view a RenderPlan, start a scoped render, and observe progress/error states. Record the observed route and UI evidence.

## 7. Documentation, real media evidence, and final verification

- [x] 7.1 Update implementation documentation for timeline contracts, source-range timing, MotionPlan rules, crop/aspect fitting, Chapter/Project rendering, music/subtitle boundaries, cache/invalidation, restart/failure behavior, and legacy mode. Verify docs match shipped API/schema names and contain no future AI-video claims.
- [x] 7.2 Add the useful permanent AGENTS rules for accepted/current Scene image inputs and chapter-local render invalidation without changing unrelated guidance. Verify rules are English, use hyphens instead of em dashes, and do not prescribe future AI video.
- [x] 7.3 Prepare a real project fixture with at least three Chapters, multiple current accepted Scene images per Chapter, current narration/audio, generated subtitles, timing, MotionPlans, and a mix of motion/transitions. Verify all prerequisites are current before rendering.
- [x] 7.4 Render one real Chapter and inspect the MP4 with ffprobe and actual playback for Scene changes, subtle motion, crop/aspect, narration, burned subtitles, transition behavior, duration, and no unexpected black frames. Record command output and observations.
- [x] 7.5 Render three real Chapters into one Project Video, validate all streams/duration/profile with ffprobe, watch the MP4, and verify project music is applied once with narration priority when enabled. Record output path, duration, file size, and quality observations.
- [x] 7.6 Change one accepted Scene image in Chapter 2, rerun the scoped plan/render, and verify through persisted fingerprints/jobs that only that Scene Clip, Chapter 2 Video, and dependent Project Video rebuild while Chapters 1 and 3 reuse their outputs. Record the before/after evidence.
- [x] 7.7 Stop and restart the worker between hierarchical stages, then retry one intentionally failed Scene Clip. Verify completed lower-level outputs survive restart, only the failed unit reruns, dependents resume, and no partial Asset becomes current.
- [x] 7.8 Run focused media/workflow/API/UI tests, the 100-Chapter planning simulation, `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test`, and `pnpm build`. Verify failures are fixed at their root and no generated files under `.omp/skills/` or `.omp/commands/` are edited.
- [x] 7.9 Run `/ponytail-review` on the final diff and remove unnecessary abstractions, duplicate paths, and speculative dependencies before any commit. Verify the final change reports READY_FOR_AI_VIDEO only after all Prompt #12 acceptance evidence is complete.

## Verification evidence

- Browser route: `http://127.0.0.1:5174/` -> `Timeline real evidence` -> `Timeline`. Invalid timing showed `Các cảnh phải nối tiếp...` and kept server revision 1. Valid `0-2501-4849 ms` timing saved from the UI as revision 2 `MANUAL`; Motion preview toggled; RenderPlan showed `Scene: 0/2 dùng lại`, `Chapter: 0/1 dùng lại`, and `Dự kiến: 4.85s`; scoped render returned `202`, displayed `SCENE_CLIP: 1/2`, then completed all four jobs. Screenshot: `C:\Users\Admin\AppData\Local\Temp\omp-sshots-156fee2eab8546db.webp`.
- Fixture: project `fe443291-2a0b-4ea8-a20a-f56aa1c813e7` with three Chapters, six current production-ready manual Scene images, three measured narration WAVs (`4849`, `4844`, `4779 ms`), three SRT files, six MotionPlans, `CROSSFADE 400 ms`, and `project:music` WAV (`768078` bytes).
- Chapter render: `apps/api/workspace/timeline-evidence/projects/fe443291-2a0b-4ea8-a20a-f56aa1c813e7/video/chapters/74c17a84-7018-48c8-9028-a8a2e7ab4f01-094f358d-59c2-4e36-8936-32cd25c3d357.mp4`, `466101` bytes. `ffprobe`: H.264 `1920x1080`, `30/1`, AAC, `4.866667 s`. Chrome playback reached `readyState 4`, `1920x1080`, `currentTime 0.458987`, `paused false`.
- Three-Chapter project render: `apps/api/workspace/timeline-evidence/projects/fe443291-2a0b-4ea8-a20a-f56aa1c813e7/video/projects/fe443291-2a0b-4ea8-a20a-f56aa1c813e7-4a4bd157-2b15-4407-9f3d-1a7b1dbb5c10.mp4`, `1611007` bytes. `ffprobe`: H.264 `1920x1080`, `30/1`, AAC, `14.500000 s`; Chrome playback reached `readyState 4`, `1920x1080`, `currentTime 0.361584`, `paused false`. The persisted `PROJECT_VIDEO` payload had `musicEnabled=1`, `loopMusic=1`, and a `musicPath`.
- Chapter 2 image invalidation: before replacement, Chapter 1 and Chapter 3 Scene/Chapter outputs were current. After replacing Chapter 2 Scene 1 image revision 2, its old `SCENE_CLIP`, Chapter 2 `CHAPTER_VIDEO`, and full-Story `PROJECT_VIDEO` became non-current/invalidated; Chapter 2 Scene 2, Chapters 1 and 3 stayed current. The persisted render-job rows recorded the same scoped invalidation.
- Worker recovery and retry: with worker stopped, `/api/health` was `degraded` and Scene Clip job `84394ab1-ee44-423a-90c9-2ede7a50d788` stayed `PENDING`; after restart health was `ready` and it completed with attempt 1. Deliberately removing the current image caused job `76f99f24-1f0e-4dac-aeff-ba682e8aece7` to fail after 3 attempts with `ENOENT` and no output asset; restoring the file, `POST /api/jobs/.../retry` returned `202`, and the same job completed at attempt 4 with output asset `59538a81-fb06-43d3-a344-84bf7ad95791`.
