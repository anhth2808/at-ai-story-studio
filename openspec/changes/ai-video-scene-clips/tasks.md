## 1. Shared contracts

- [x] 1.1 Add `packages/shared/src/video.ts`: strict Zod schemas for `VideoGenerationRequest`, `VideoGenerationResult`, `VideoGenerationSettings` (provider, base URL, model components, sampler/scheduler/steps/guidance/shift, preset, timeouts, approval flag), `videoReadinessSchema` (separate statuses incl. `VIDEO_MODEL_MISSING`, `WORKFLOW_MISSING`), `AiMotionPlan` + update DTO (action/environment/camera vocab, `SUBTLE|MEDIUM|STRONG`, priority), review DTO with video issue tags, `motionSourceSchema` (`KEN_BURNS|AI_VIDEO|HYBRID`), and generation/history DTOs. Verify invalid enums/ranges/URLs fail schema tests and no provider node ids are representable.
- [x] 1.2 Extend `workflowStepTypeSchema` with `GENERATE_AI_SCENE_VIDEO`, `assetTypeSchema` with `AI_SCENE_VIDEO`, `renderPlanSchema` with the `ai` block (selected/missing/clipsToNormalize/estimatedGenerations/estimatedGenerationMs), and `SceneTimelineItem` with `motionSource` + `aiMotion` status. Verify existing timeline/image/story schema tests still parse prior payloads unchanged.
- [x] 1.3 Add preset constants (`LOW_VRAM`, `BALANCED`, `QUALITY`: width/height/frames/steps/guidance) and the duration-policy contract (`AI_THEN_KEN_BURNS`, crossfade bounds) in shared. Verify preset dimensions are even, frame counts bounded, and orientation matches 16:9/9:16 project profiles.

## 2. ComfyUI video provider

- [x] 2.1 Implement `packages/workflow/src/comfyui-video.ts`: `buildComfyUiVideoPrompt()` producing the Wan 2.2 TI2V-5B graph with fixed node ids (37/38/39/48/6/7/56/55/3/8/57/58), `videoGraphSpec()` validation (node ids, class types, inputs, links), mapping version `image-to-video-v1-mapping-1`, template negative prompt, and preset->`Wan22ImageToVideoLatent` width/height/length mapping. Verify a tampered graph fails validation before any HTTP call and the mapping sets image/prompt/seed/resolution/frames from the request only.
- [x] 2.2 Implement `ComfyUiVideoProvider implements VideoGenerationProvider` reusing the ComfyUI HTTP conventions: source-image upload via `/upload/image`, idempotent submit (persisted provider job id checked in `/history/{id}` then `/queue` before POST `/prompt`), 2s polling, long configurable timeout, `/view` MP4/WEBM download into `staging/comfyui-video-{jobId}/`, targeted cancellation with honest limitation reporting, and provider error codes incl. OOM classification. Verify client tests with fake fetch cover submit/resume/download/cancel/OOM paths.
- [x] 2.3 Implement `readiness()` for video: reachability, required native node presence (`Wan22ImageToVideoLatent`, `CreateVideo`, `SaveVideo`, `ModelSamplingSD3`), model file membership in `/models/diffusion_models|text_encoders|vae`, sampler/scheduler availability, preset validity. Verify image readiness remains a separate result and a server missing only Wan models reports `VIDEO_MODEL_MISSING` while image reports `READY`.

## 3. Database migration and repositories

- [x] 3.1 Add additive migration `0014_ai_video.sql` after `0013_animated_story_timeline.sql`: `video_generation_settings`, `ai_motion_plan_revisions`, `scene_video_generations` (request snapshot, source image asset id + sha, motion plan revision, provider/job/seed/status/attempt/error/duration, review fields, partial-unique current per scene+source-image), and Scene motion-source storage per the design (payload field or nullable column). Verify fresh and existing databases migrate without reset and legacy rows parse.
- [x] 3.2 Add Drizzle schema types and `SceneVideoRepository`/`AiMotionPlanRepository` following the timing/motion repository patterns: transactional revision creation, current-pointer rotation, stale detection against source image sha, review updates, generation-duration stats for estimates. Verify repository tests cover rollback, current replacement, and history addressability.
- [x] 3.3 Extend asset registration for `AI_SCENE_VIDEO` under `projects/{pid}/video/motion/{sceneStableId}/{generationId}.mp4` (extend `managedVideoRelativePath` kind, `prepareProjectDirectories`) with the same promotion/path-safety guarantees. Verify traversal attempts and overwrites are rejected and raw assets are never deleted by normalization.

## 4. Motion planning and generation service

- [x] 4.1 Implement `packages/workflow/src/ai-motion-plan.ts`: deterministic `createDefaultAiMotionPlan()` from scene purpose/camera/composition/movement-intent, `compileMotionPrompt()` emitting motion-only text with safety phrasing, and intensity->phrasing mapping. Verify identical scene inputs produce identical plans/prompts and prompts contain no visual identity dump.
- [x] 4.2 Implement `SceneVideoService` scheduling following `ImageGenerationService`: request snapshot + fingerprint persistence, `GENERATE_AI_SCENE_VIDEO` step + job creation (key `scene-video:{sceneRevisionId}:{revision}`), seed resolution, batch endpoints semantics (sceneIds / chapter-missing), regenerate (SAME_SEED/NEW_SEED + review feedback guidance) distinct from retry. Verify scheduling requires a current accepted Scene image and rejects scenes without one with a named prerequisite.
- [x] 4.3 Implement `executeStep` for generation: staleness rechecks (scene, image currency + sha, motion plan fingerprint, settings fingerprint), `markRunning`, provider call, mp4 validation via ffprobe (stream/duration/dimensions/fps), staging->managed promotion, `commitGenerated` with StepLeaseGuard, review-gated current selection. Verify stale-lease and changed-image completions persist as history only and corrupt output never publishes.
- [x] 4.4 Wire worker dispatch for `GENERATE_AI_SCENE_VIDEO` with the existing heartbeat/cancellation/lease/recovery machinery and the video timeout. Verify a worker restart mid-generation reconciles the persisted provider job id instead of duplicate-submitting and an OOM failure does not auto-retry identical settings.

## 5. SceneClip normalization and integration

- [x] 5.1 Implement `packages/media/src/ai-clip-render.ts`: pure `buildAiSceneClipArguments()` for trim, scale/fit to profile, `-an`, and `AI_THEN_KEN_BURNS` composition (segment A raw clip, segment B bounded Ken Burns over the accepted image, `xfade` crossfade within bounds) with input validation mirroring `timeline-render.ts`. Verify compiler tests assert argument arrays and reject odd dimensions, missing inputs, zero crossfade overlap, and unsafe paths before invocation.
- [x] 5.2 Extend `sceneClipFingerprint()` with the AI branch (`ai-scene-clip-ffmpeg-v1`: raw generation fingerprint, timing, policy, crossfade, profile, fit) selecting by scene motion source; keep the Ken Burns fingerprint byte-identical. Verify KEN_BURNS scenes produce unchanged fingerprints vs #12 and AI scenes change only on raw/policy/profile/timing inputs.
- [x] 5.3 Extend `executeSceneClipRender` with the AI source branch: resolve the accepted current raw clip by scene + source-image sha, probe it, compile via 5.1, run FFmpeg to staging, `validateHierarchicalVideo` (no audio, tolerance as clips), promote, register `SCENE_VIDEO_CLIP` under `scene:{stableId}:video` with the AI fingerprint and dependency on the raw asset. Verify AI-mode scenes without an accepted current clip block with a named prerequisite or use the explicit Ken Burns fallback per policy, never a silent substitute.
- [x] 5.4 Wire scoped invalidation: image change stales dependent raw generations (lineage, not deletion) and the existing chain handles clip/chapter/project; motion plan change and regenerate-accept invalidate the clip role only; timing/subtitle/music/quality changes leave raw assets current. Verify integration tests prove raw reuse (zero provider calls) for timing-only, subtitle-only, and quality-preset-only changes.

## 6. RenderPlan and scheduling integration

- [x] 6.1 Extend `getRenderPlan`/`getSingleSceneRenderPlan` with the `ai` block: selected AI-mode scenes, missing accepted motion, clips needing normalization, estimated generation count, and median-duration estimate from recent completed generations (nullable). Verify a 100x5 mixed-mode plan stays metadata-only, never schedules generation, and reports correct counts in the scale simulation.
- [x] 6.2 Keep `POST /render` generation-free: AI-mode scenes without motion surface as blockers or explicit fallback per `fallbackPolicy`. Verify a full-story render with missing AI motion neither invokes the provider nor silently degrades unselected scenes beyond the declared policy.

## 7. API surface

- [x] 7.1 Add thin routes: `GET/PUT /api/projects/:id/video-settings`, `POST .../video-settings/readiness`, `GET/PUT .../scenes/:sceneId/ai-motion` (expectedRevision), `POST .../scenes/:sceneId/ai-video/generate`, `POST .../ai-video/:generationId/regenerate|review|current`, `GET .../ai-video/:generationId`, `POST .../ai-video/generate-batch`, `POST .../chapters/:chapterId/ai-video/generate-missing`. Verify 202 + job ids convention, ownership checks, strict bodies, no absolute paths in responses, and retry/cancel flow through the existing `/api/jobs/:id/*`.
- [x] 7.2 Extend timeline/scene/status DTOs with motion source, AI status, raw/normalized asset URLs, and video readiness. Verify existing timeline consumers parse extended payloads and scene video metadata endpoints serve both clip kinds through `/api/assets/:id`.

## 8. Web UI

- [x] 8.1 Add `videoApi` client namespace and state handling following `imageApi` conventions. Verify typed calls match routes and stale-request guards follow the existing poller pattern.
- [x] 8.2 Scene card: motion source picker, motion prompt editor (expectedRevision), seed/status display, Tạo video AI / Thử lại / Tạo lại actions, raw clip preview, normalized clip preview, review form with video issue tags + notes, accept/set-current, and readiness display with actionable missing-model guidance. Verify generation requires explicit action, review is required before production use when approval is on, and Vietnamese copy/accessibility follow existing conventions.
- [x] 8.3 Timeline: per-scene source badge (Ken Burns/AI Video/Hybrid), AI status, and playable normalized SceneClip preview (wire the unused `sceneClipAssetUrl`). RenderPlan display gains the AI counts/estimate line labeled as estimate. Verify mixed-mode chapters render badges correctly and no separate AI timeline view exists.
- [x] 8.4 Batch UI: select scenes / generate-missing-in-chapter with explicit action, per-scene failure isolation display, and retry. Verify a mid-batch failure leaves completed scenes valid and shows a retry for the failed one only.

## 9. Tests and simulation

- [x] 9.1 Add `FakeVideoProvider` (deterministic real MP4 via FFmpeg lavfi honoring requested dims/fps/duration) and wire it into service/workflow tests. Verify no unit test depends on GPU generations.
- [x] 9.2 Add focused tests: request mapping (preset->graph inputs), workflow spec validation, raw fingerprint stability/sensitivity, normalized fingerprint stability/sensitivity, AI staleness and source-image invalidation, timing-change reuse, subtitle-change reuse, HYBRID duration composition math (argv-level), Ken Burns fallback paths, retry-vs-regenerate semantics, stale in-flight result, restart reconciliation (fake provider job id reuse), batch scheduling caps. Verify each fails on the plausible bug it defends against.
- [x] 9.3 Add the 100-chapter x 5-scene scale simulation (70% KEN_BURNS / 20% HYBRID / 10% AI_VIDEO) with FakeVideoProvider asserting plan counts, dependency fan-out, invalidation locality, and zero provider calls for render-only changes. Verify the simulation runs without real GPU work.
- [x] 9.4 Run `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test`, `pnpm build`; fix root causes. Verify the #12 timeline suite, image suites, and story/TTS suites remain green.

## 10. Real local verification (GPU)

- [x] 10.1 Benchmark gate: generate real Wan 2.2 5B clips for 3 representative accepted Scene images (character medium shot, environment wide shot, action scene) at LOW_VRAM and BALANCED presets; record resolution/frames/fps/clip duration/generation wall time/VRAM behavior/errors/visual observations in `docs/implementation/video-benchmark.md`; freeze the default preset from measurements and declare `RTX3060_12GB_PRACTICAL` honestly.
- [x] 10.2 Identity/motion review evidence: compare source image vs generated clips for face/hair/clothing/object/background stability and motion usefulness across the 3 benchmark clips; document typical failure modes and the chosen defaults.
- [x] 10.3 Real HYBRID test: one scene with narration duration >= 20s, real 3-5s AI clip, HYBRID SceneClip built to exact duration; verify no black gap, bounded crossfade, exact duration/FPS/resolution via ffprobe and playback.
- [x] 10.4 Mixed Chapter render: one real Chapter with >= 1 KEN_BURNS + 1 AI_VIDEO + 1 HYBRID scene through the unchanged Chapter renderer; verify narration/subtitles/timing behavior.
- [x] 10.5 Multi-Chapter Project: >= 3 Chapters, >= 1 with AI SceneClips, rendered through the normal #12 pipeline; verify no special AI renderer exists or is used.
- [x] 10.6 Scoped rerender evidence: regenerate one AI motion asset in Chapter 2; prove via fingerprints/jobs that only its clip, Chapter 2, and the Project rebuild while Chapters 1/3 reuse.
- [x] 10.7 Expense-reuse evidence: (a) timing-only change on an AI scene reuses the raw asset with zero provider calls; (b) subtitle change reuses raw + clips; (c) quality preset change reuses raw. Capture provider-call-proof (no new generations rows/jobs).
- [x] 10.8 Durability evidence: restart worker during a real generation and verify reconciliation/resume per design; force a provider failure and verify FAILED + no current asset + Ken Burns fallback render + independent retry; start generation against image v1, swap current image to v2 mid-flight, verify the v1 result stays non-current history.

## 11. Documentation and final review

- [x] 11.1 Write/update `docs/implementation/`: `ai-video.md`, `video-provider.md`, `comfyui-video.md`, `ai-motion-plan.md`, `hybrid-scene-clips.md`, `video-benchmark.md`; update `animated-story-timeline.md`, `architecture.md`, `workflow.md`, `known-limitations.md`, `setup.md` (model files, sizes, locations, license), and remove documentation drift contradicting #12/#13 reality.
- [x] 11.2 Add durable AGENTS.md rules: AI Video is a SceneClip source and must not own Chapter/Project rendering; raw expensive AI motion assets must remain reusable when only timing, subtitle, music, or final render settings change; Ken Burns remains a first-class fallback. Verify English, hyphens, no scope creep.
- [x] 11.3 Run `/ponytail-review` over the final diff and remove unneeded abstractions/dependencies before commit. Verify the final report states `SELECTED_VIDEO_TECHNIQUE`, `RTX3060_12GB_PRACTICAL`, and `READY_FOR_PRODUCTION_PIPELINE` only from recorded evidence.
